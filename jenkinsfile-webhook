pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[credentialsId: 'github_token', url: 'https://github.com/DEL-ORG/s6kinga.git']])'
            }
        }
    }
}
